# 运行环境：development / test / production
NODE_ENV=development

# HTTP 监听。容器平台通常会覆盖 PORT。
HOST=0.0.0.0
PORT=3000

# 当前部署的平台。一个部署环境只服务一个平台，避免身份和数据混用。
PLATFORM=wechat
APP_ID=replace-with-platform-app-id

# 仅在云端 Secret/环境变量中配置，禁止提交真实值。
APP_SECRET=replace-with-platform-app-secret

# 日志级别：debug / info / warn / error
LOG_LEVEL=info

# development/test 可用 memory；production 强制使用 cloudbase-http。
PERSISTENCE_DRIVER=memory

# 腾讯 CloudBase 内置文档数据库 HTTP API 配置。
# API Key 只能存于本地 .env 或云端 Secret，禁止提交真实值或下发前端。
CLOUDBASE_ENV_ID=replace-with-cloudbase-env-id
CLOUDBASE_REGION=ap-shanghai
CLOUDBASE_API_KEY=replace-with-server-api-key
# CloudBase 托管平台也可能以 CLOUDBASE_APIKEY 或 TCB_API_KEY 注入；代码已兼容，三者只需配置一个。
CLOUDBASE_DATABASE_INSTANCE=(default)
CLOUDBASE_DATABASE_NAME=(default)

# 管理员认证：账号和初始密码必须同时配置才启用 /admin/v1/auth/*。
# 初始密码至少 12 个字符，只能放在本地或云端 Secret，禁止提交真实值。
ADMIN_BOOTSTRAP_ACCOUNT=
ADMIN_BOOTSTRAP_PASSWORD=
ADMIN_BOOTSTRAP_DISPLAY_NAME=超级管理员
ADMIN_WEB_ORIGIN=http://127.0.0.1:5173

# 通用文件上传使用腾讯云 COS。密钥只配置在服务端 Secret，禁止下发前端或提交 Git。
COS_SECRET_ID=
COS_SECRET_KEY=
COS_BUCKET=miao-1487859276
COS_REGION=ap-guangzhou
# 可先使用 COS 默认 HTTPS 域名；配置自定义域名后只需替换此项。
COS_PUBLIC_BASE_URL=https://miao-1487859276.cos.ap-guangzhou.myqcloud.com
