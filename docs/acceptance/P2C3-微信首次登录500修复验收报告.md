# P2C3 微信首次登录 500 修复验收报告

日期：2026-09-16

## 已完成验收

- 玩家文档不存在：返回 `undefined`，允许登录服务创建新玩家。
- 会话文档不存在：返回 `undefined`，允许鉴权层按无会话处理。
- 未知数据库错误：继续抛出，不被误吞。
- 探针与生产仓储共用 CloudBase 错误识别规则。
- 自动化测试：8 个测试文件、26 个用例全部通过。
- ESLint、TypeScript 构建和 `git diff --check` 通过。

## 尚待云端验收

- 新版本部署并切换流量。
- 微信开发者工具首次真实登录返回 200、`isNew=true`。
- 同一账号使用新的登录 code 再次登录，playerId 相同且 `isNew=false`。
- `players`、`sessions` 中不出现明文 openid、unionid 或 token。

在以上真实链路通过前，P2C3 状态为“本地验收通过，待云端验收”。
