# P2C3 微信首次登录 500 修复验收报告

日期：2026-09-16

## 已完成验收

- 玩家文档不存在：返回 `undefined`，允许登录服务创建新玩家。
- 会话文档不存在：返回 `undefined`，允许鉴权层按无会话处理。
- 未知数据库错误：继续抛出，不被误吞。
- 探针与生产仓储共用 CloudBase 错误识别规则。
- 自动化测试：8 个测试文件、26 个用例全部通过。
- ESLint、TypeScript 构建和 `git diff --check` 通过。

## 云端验收

- 修复版本已部署并切换流量。
- 微信开发者工具首次真实登录返回 HTTP 200、业务 `code=0`、`isNew=true`。
- 同一微信账号重启小游戏并使用新的登录 code 再次登录，HTTP 200、`isNew=false` 且 playerId 与首次一致。
- 登录响应和 CloudBase 调用链均返回成功；服务端不向响应返回 openid、unionid 或 session key。

P2C3 状态：云端验收通过。聊天截图曾包含一次完整会话 token，该测试会话应删除或撤销，不作为正式账号会话继续使用。
