# P5B1 微信头像昵称资料验收报告

日期：2026-09-18

## 后端执行

- 抽取共享 Bearer 会话鉴权，云存档和资料接口使用同一套 token 哈希、撤销和过期规则。
- 新增 `GET/PUT /v1/profile`、输入校验、请求日志脱敏、内存仓储和 CloudBase 仓储。
- 新增 `player_profiles` 建表检查与 `profile:check:wechat` 临时资料探针。
- API、后端计划、部署说明和管理后台计划已同步。

## 自动验收

- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm test`：14 个测试文件、56 项测试全部通过。
- `npm run build`：通过。
- 覆盖空资料、首次保存、再次读取、无会话、非法昵称、非 HTTPS 头像、额外字段剔除和 CloudBase 文档映射。

## 真实 CloudBase 验收

- `db:check:wechat`：通过，确认 `players/sessions/cloud_saves/remote_configs/player_profiles` 均存在且数据库可写读清理。
- `profile:check:wechat`：通过，随机测试资料完成首次写入、读取、更新和最终删除；未接触现有玩家文档。

## 尚待部署后验收

- CloudBase Run 更新 Git 平台部署。
- 前端 Cocos Creator 3.8.8 微信构建已通过，日志为 `build/logs/wechat-build-20260918-174421.log`。
- 微信开发者工具点击资料页刷新按钮，确认原生授权返回资料且 `PUT /v1/profile` 为 200。
- 关闭并重新启动，确认 `GET /v1/profile` 为 200，昵称、资料页头像和首页头像恢复一致。
- 取消授权和资料接口失败时，确认旧资料不被清空且游戏继续可用。

当前结论：代码、自动检查和真实数据库探针通过；真实微信授权链路需部署后由开发者工具完成最终验收。
