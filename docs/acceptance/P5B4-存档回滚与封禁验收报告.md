# P5B4 存档回滚与封禁验收报告

## 自动验收

- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm test`：17 个测试文件、70 项测试全部通过。
- `npm run build`：通过。

覆盖场景：

- 当前/上一版摘要及 user 字段差异。
- expectedRevision 成功回滚后 revision 单调递增。
- 过期 expectedRevision 返回 409，不覆盖新版本。
- 临时封禁、解封及审计日志。
- 运营角色永久封禁返回 403。
- 已有玩家会话封禁后云存档请求返回 403 `PLAYER_BANNED`。

## 验收边界

- 本地服务当前没有测试玩家，管理写接口以 Fastify 真实路由注入和内存仓储验收。
- 未对线上真实玩家执行封禁或存档回滚。
- CloudBase 隔离玩家的真实条件回滚与游戏客户端提示仍待部署阶段验证，A3 暂不标记生产最终完成。
