# P5B4 存档回滚与封禁验收报告

## 自动验收

- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm test`：17 个测试文件、70 项测试全部通过。
- `npm run build`：通过。

覆盖场景：

- 当前/上一版摘要及 user 字段差异。
- expectedRevision 成功回滚后 revision 单调递增。
- 过期 expectedRevision 返回 409，不覆盖新版本。
- 临时封禁、解封及审计日志。
- 运营角色永久封禁返回 403。
- 已有玩家会话封禁后云存档请求返回 403 `PLAYER_BANNED`。

## 2026-09-20 CloudBase 隔离玩家验收

- 创建唯一隔离玩家并写入 revision 1、2 两版存档。
- 存档诊断返回当前 revision=2、上一版 revision=1 和 2 个字段变化。
- 使用 expectedRevision=2 回滚成功，新 revision=3；再次使用旧 revision 返回 409 `SAVE_ROLLBACK_CONFLICT`。
- 临时封禁后，已有玩家会话读取云存档返回 403 `PLAYER_BANNED`；解封后恢复 HTTP 200。
- 审计日志包含 `save.rollback`、`player.ban` 和 `player.unban`。
- 验收结束后清理隔离玩家、资料、会话、存档及关联审计，唯一测试昵称复查剩余 0 条。

## 当前边界

- 未对线上真实玩家执行封禁或存档回滚。
- 游戏客户端封禁提示和回档后的真实同步体验仍需微信端单独验收。
