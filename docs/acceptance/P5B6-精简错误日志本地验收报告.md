# P5B6 精简错误日志本地验收报告

## 验收时间

2026-09-20

## 自动验收

- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm test`：18 个测试文件、75 项测试全部通过。
- `npm run build`：通过。
- `git diff --check`：通过。
- `npm run db:check:wechat`：通过，9 个集合可访问，新增 `admin_error_logs`；探测记录已删除。
- `npm run error-log:check:wechat`：真实写入、详情读取和时间倒序列表读取通过；探测记录已删除。

## 已验证场景

- 已知业务错误自动写入脱敏日志，并保留 requestId、接口模板、HTTP 状态和业务错误码。
- 按完整 requestId、业务错误码和时间范围组合查询成功。
- 单条错误详情查询成功，缺失 ID 返回 `ERROR_LOG_NOT_FOUND`。
- 未登录访问被拒绝；正常登录页会话探测和错误日志接口自身失败不会递归写入日志。
- 自动测试确认日志中不包含管理员密码和会话 token。
- 真实探测发现并修复 CloudBase SDK 对文档顶层 `code/message` 的协议字段冲突，数据库改存 `errorCode/safeMessage`，管理 API 契约不变。
- 本地内存后端与管理后台完成端到端页面验收：受控 `INVALID_CURSOR` 错误能够按 requestId 查询并读取详情，退出登录成功；内存测试日志随进程退出清除。

## 验收边界

- 当前报告已完成本地代码、内存仓储和 CloudBase 集合探测；云端版本部署和公网接口验收需要在提交推送后继续执行。
- 本阶段不包含监控指标、告警、工单处置状态和日志删除页面。
