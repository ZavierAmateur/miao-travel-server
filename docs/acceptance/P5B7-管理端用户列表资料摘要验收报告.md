# P5B7 管理端用户列表资料摘要验收报告

## 验收日期

2026-09-20

## 验收范围

- `GET /admin/v1/players` 返回当前页用户的资料摘要。
- 未改变管理员权限、分页、筛选和敏感数据边界。
- 同步管理后台列表契约。

## 自动验收

- 管理端玩家 API 测试验证列表返回 `createdAt`、昵称、头像、资料更新时间和存档摘要。
- 测试继续确认响应不包含平台 openid 和云存档正文。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm test`：18 个测试文件、75 项测试通过。
- `npm run build`：通过。
- `git diff --check`：通过。

## 部署边界

代码提交推送后需要更新 CloudBase Run Git 平台部署，管理后台列表才能从线上接口取得 `profile`。部署前前端会正常显示创建时间，昵称头像显示“未授权”占位。
