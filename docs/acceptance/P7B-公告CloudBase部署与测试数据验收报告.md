# P7B 公告 CloudBase 部署与测试数据验收报告

日期：2026-09-21

## 审计范围

- CloudBase Run `miao-travel-wechat` 新版本健康状态。
- 线上管理员登录、公告分页列表和公告详情接口。
- 两条测试公告的真实 CloudBase 写入及回读。
- 草稿公告对小游戏公共接口的隔离。

## 执行结果

- `GET /health`：HTTP 200。
- `POST /admin/v1/auth/login`：HTTP 200，管理员会话签发成功。
- 新增两条仅微信平台使用的草稿公告，均关闭自动弹出：
  - `3e89257d-9d39-4451-ad6d-bcd82eeea0e1`：`[测试] 欢迎来到喵的旅行日记`，排序 100。
  - `bf642b20-ac78-4fa6-8b70-3bcb9095fcff`：`[测试] 旅途补给说明`，排序 90。
- 两条测试公告均包含富文本正文，当前图片列表为空。

## 验收证据

- 管理端按 `draft + wechat + 测试` 查询：HTTP 200，`total = 2`，顺序与排序值一致。
- 两个公告详情分别回读：HTTP 200，ID、标题、状态和正文均与写入数据一致。
- 小游戏公共列表 `GET /v1/announcements?platform=wechat&page=1&pageSize=10`：HTTP 200，`total = 0`。
- 公共列表未返回草稿，证明发布状态过滤生效。
- 验收过程未记录管理员密码、会话 Cookie、CloudBase API Key 或 COS 密钥。

## 待继续验收

- 在管理后台将测试公告切换为“已发布”后，继续验收小游戏公共列表与详情展示。
- 当前两条公告没有图片；COS 上传接口和小游戏图片下载需另行使用测试图片验收。

结论：公告后端已在 CloudBase Run 正常运行；两条草稿测试公告已真实写入 CloudBase 并回读通过，且不会提前暴露给小游戏用户。
