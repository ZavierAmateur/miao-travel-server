# P8B 闯关榜筛选本地验收报告

日期：2026-09-22

## 执行内容

- 闯关榜接口新增 `nickName`、`platform`、`playerId` 查询参数。
- 微信昵称支持包含匹配，平台和用户 ID 使用精确匹配。
- 筛选条件支持组合，分页固定每页 20 条。
- 筛选结果返回玩家在全榜中的真实排名。

## 自动验收

- 接口专项测试覆盖昵称、平台、用户 ID、组合筛选、无结果和非法平台。
- CloudBase 仓储测试覆盖跨 100 条批次筛选并保留真实排名。
- `npm test`：22 个测试文件、92 项测试全部通过。
- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm run build`：生产 TypeScript 构建通过。

## 待生产验收

- 部署 CloudBase 云托管新版本。
- 管理员登录后分别使用三个条件以及组合条件验收线上返回结果。
