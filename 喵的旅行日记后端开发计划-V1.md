# 喵的旅行日记后端开发计划（V1）

> 文档日期：2026-09-16
>
> 前端工程：`/Volumes/work/Projects/cocos/喵的旅行日记`
>
> 后端工程：`/Volumes/work/Projects/Server/miao-travel-server`
>
> 目标平台：微信小游戏、抖音小游戏
>
> 定位：在免费/低配云资源约束下，优先解决账号识别、跨设备云存档和基础运营能力；保持离线可玩，不建设重型游戏服务器。

## 1. 前端项目审计结论

### 1.1 技术与运行结构

- Cocos Creator 3.8.8、TypeScript、FairyGUI；前端版本为 `3.4.2`。
- 启动主链为 `Main -> LoadingActivity -> GameStartupCoordinator -> ControllerManager -> Home/GameActivity`。
- 已有微信和抖音构建产物，平台抽象包含 `PlatformKind.WeChat`、`PlatformKind.ByteDance`，小游戏登录适配器能统一取得临时 `code`。
- 远程渠道配置已经支持 `dev/prod` 的 `appId`、`baseUrl`，适合按平台和环境配置后端地址。
- 游戏是客户端单机玩法：地图、候选方块、订单、路径、胜负判定均在前端完成。V1 后端没有必要承载实时对局、房间、帧同步或 WebSocket。

### 1.2 当前存档体系

- 本地存档按 `miao-travel-diary:{userId}:{moduleKey}` 分模块保存，并使用加密 JSON 和完整性校验。
- 持久化模块包括：
  - `user`：等级、体力、金币、星星、道具、动物收集、广告/签到/购买次数等玩家主状态；
  - `settings`：声音、音乐、振动及每日状态；
  - `tutorial`：新手引导进度；
  - `mission`：任务进度；
  - `engagement`：每日礼包、关卡宝箱、存钱罐等活动状态；
  - `record`：完整的局内续玩快照。
- `ControllerManager.getUserStorage()` 已能导出统一云存档结构：

```ts
type UserStorage = {
  version: string;
  serialized: 0 | 1;
  time: number;
  modules: Record<string, unknown>;
};
```

- 前端已有云存档选择规则：仅当服务端存档结构有效且 `server.time > local.saveTime` 时覆盖本地；版本迁移失败会回滚本地快照。
- `ServerController` 已有前后台保存、定时保存、服务端脏标记和心跳位置，但目前全部为禁用 stub；`zysdk/http.ts`、`zysdk/user.ts` 同样被禁用。
- 当前定时常量是每 2 分钟检查上传、每 1 分钟心跳。V1 不应原样启用心跳，否则会浪费免费调用额度。

### 1.3 已有后端契约雏形

登录成功后前端已经准备接收：

```ts
{
  token: string;
  playerId: string;
  isNew: boolean;
  openId: string;
  unionId: string;
  data: string;       // 历史接口为 JSON 字符串
  isBanned: boolean;
  banReason: string;
  banExpire: number;
  whiteList: boolean;
}
```

随后会设置 `Env.token/playerId/openId`、切换本地存档命名空间、把服务端存档交给 `ControllerManager.checkServerData()`。因此 V1 应优先恢复现有契约，再逐步把 `data` 从字符串兼容形态升级为明确对象契约。

### 1.4 对后端范围的直接结论

- 必须做：平台登录换取身份、自有会话、云存档读写、并发冲突保护、服务端时间、限流、日志与备份。
- 适合 V1 做：公告/远程开关、兑换码（仅当首发确实需要入口）。
- 暂不做：实时心跳、邮件、排行榜、好友、社交、公会、实时对战、支付订单、复杂反作弊、服务端重演关卡。
- 客户端存档可被修改，V1 云存档只能提供“同步与恢复”，不能宣称资源完全防作弊。涉及付费、稀缺奖励或排行榜时，再把对应经济操作改为服务端权威事务。

## 2. V1 产品目标与非目标

### 2.1 目标

1. 微信、抖音玩家分别可用平台账号静默登录。
2. 换设备或清缓存后可恢复最新云存档。
3. 断网、云服务故障、配额耗尽时仍可使用本地档进入游戏。
4. 同一账号多设备并发保存时不静默覆盖较新的云档。
5. 后端空闲时尽量不产生常驻计算费用，数据库读写次数可控。
6. 同一套业务代码支持两个平台部署，平台差异只存在于身份适配和部署配置层。

### 2.2 非目标

- 不做跨微信、抖音的账号合并；两个平台的 `openid` 不是同一身份。
- 不把局内每一步上传服务端，也不做在线强制校验。
- 不因登录失败阻断启动，不要求游戏全程联网。
- 不在 V1 引入 Redis、消息队列、Kafka、独立网关、微服务或常驻心跳。

## 3. 推荐总体架构

```text
微信小游戏                         抖音小游戏
 wx.login()                        tt.login()
     |                                 |
     v                                 v
微信部署环境                       抖音部署环境
 Auth Adapter                       Auth Adapter
     \                                 /
      +---- 同一套 TypeScript API 核心 ----+
            登录 / 云存档 / 配置 / 兑换码
                    |
              Repository 接口
                    |
       各平台云数据库（数据物理隔离）
```

推荐形态：

- 单仓库、单体模块化后端，不拆微服务。
- Node.js 20 + TypeScript；HTTP 层建议 Fastify（依赖少、校验清楚、适合函数或容器）。
- `platform/wechat`、`platform/bytedance` 只负责 code 换 session；业务层不依赖具体云厂商 SDK。
- `repository` 层隔离数据库实现，先为各平台的托管数据库提供适配器。
- 以无服务器函数或可缩容到零的托管服务优先；不要配置最低常驻实例、预置并发或 Redis。
- 微信、抖音分别使用独立环境变量和数据库；API 路径、请求/响应模型和错误码完全一致。

说明：部署前必须在两个平台控制台再次确认可用产品、免费额度、数据库类型、地域、最小实例数和到期策略。免费试用不等于永久免费；尤其要关闭非必要按量付费并设置预算告警。

## 4. 模块设计

### 4.1 `auth`：平台身份与自有会话

职责：

- 接收 `platform + code`（抖音兼容 `anonymousCode`）。
- 仅在后端调用微信/抖音 `code2Session`；AppSecret、session key 永不下发。
- 用 `(platform, app_id, platform_open_id)` 唯一定位玩家。
- 创建不可预测的自有访问令牌，返回 `playerId` 和服务端时间。
- 平台 code 一次性使用；前端每次冷启动最多调用一次平台登录。

V1 会话建议：随机 opaque token，数据库仅存 token 哈希，30 天滑动有效期；比自签 JWT 更方便主动失效和封禁。若平台云托管提供可信免登录身份注入，可在对应 adapter 中替换，不侵入业务层。

### 4.2 `save`：云存档

保存单位：每名玩家一份聚合存档，而不是把每个客户端模块拆成一张表。原因是 V1 的读取总是整包恢复，聚合文档能显著减少免费数据库读写和一致性复杂度。

保存记录至少包含：

```ts
interface CloudSaveRecord {
  playerId: string;
  revision: number;          // 服务端单调递增
  clientVersion: string;
  clientSavedAt: number;     // 仅供诊断，不作为权威并发依据
  serverSavedAt: number;     // 服务端生成
  payload: UserStorage;
  payloadHash: string;
  sizeBytes: number;
}
```

保存规则：

- `PUT /v1/save` 必须携带客户端上次读取到的 `baseRevision`。
- 数据库使用条件更新/事务：`baseRevision == currentRevision` 才写入，否则返回 `SAVE_CONFLICT` 和云端摘要。
- `time` 最终由服务端校准，不能信任设备时间作为覆盖依据。
- 限制 JSON 深度、模块白名单、总字节数和数字范围；拒绝 `NaN/Infinity`、原型污染键和异常超大数组。
- 首版保留当前版本和最近 1 个历史版本，用于误覆盖回滚；历史版本通过覆盖写轮换，不无限增长。
- `record` 局内续玩数据体积较大但仍随整包保存；实际压测超过阈值后再考虑独立保存。

### 4.3 `config`：轻量远程配置

- 只存公告、维护开关、最低兼容版本、功能开关、后端存档大小限制等小型 JSON。
- 读接口支持 `ETag/If-None-Match` 和 5～15 分钟客户端缓存。
- 游戏数值配置仍随客户端资源包发布；V1 不把所有 Excel 配置搬到服务端。

### 4.4 `redeem`：兑换码（可选）

前端已有兑换码弹窗，但尚未绑定真实后端。若首发需要，则纳入 V1.1：

- `redeem_codes` 保存批次、有效期、总次数/单玩家次数和奖励模板。
- 领取记录对 `(player_id, code_id)` 建唯一约束。
- 兑换、领取记录、存档 revision 更新必须在一个事务内完成。
- 请求携带 `Idempotency-Key`，重复请求返回同一结果。

如果首发没有兑换活动，整个模块不建表、不部署，减少攻击面。

### 4.5 `admin/ops`：最小运维能力

- V1 不开发完整 Web 管理后台。
- 使用云控制台或受保护的 CLI 完成封禁、查档、回滚和配置发布。
- 管理操作写审计日志；生产密钥不可进入 Git、前端包或日志。

## 5. 数据模型

### 5.1 `players`

| 字段 | 说明 |
|---|---|
| `id` | 内部不可枚举 UUID/ULID |
| `platform` | `wechat` / `bytedance` |
| `app_id` | 平台应用标识 |
| `_id` | `SHA-256(platform, app_id, platform_open_id)`，作为平台身份唯一键 |
| `platform_open_id` | 不保存明文；只在换取身份后的单次请求内使用 |
| `union_id_hash` | 可空的 SHA-256；V1 不依赖它合并账号 |
| `status` | `active` / `banned` |
| `ban_reason`, `ban_expire_at` | 封禁信息 |
| `created_at`, `last_login_at` | 服务端时间 |

唯一性由身份哈希 `_id` 保证；另对内部 `id` 建唯一索引，并为后台预留 `(platform, status, last_login_at)` 组合索引。

### 5.2 `sessions`

| 字段 | 说明 |
|---|---|
| `token_hash` | 自有 token 的安全哈希，唯一 |
| `player_id` | 玩家 ID |
| `platform_session_key_ciphertext` | 必要时加密保存；不写日志 |
| `expires_at`, `last_seen_at`, `revoked_at` | 生命周期 |

### 5.3 `cloud_saves`

`player_id` 唯一，保存 `revision/payload/hash/size/client_version/client_saved_at/server_saved_at`。

### 5.4 `cloud_save_history`

每名玩家最多 1 条上一版本；新保存时轮换。不要无限追加快照。

### 5.5 可选表

- `remote_configs`
- `redeem_codes`
- `redeem_claims`
- `admin_audit_logs`

不要在 V1 创建邮件、排行榜、订单、好友等空表。

## 6. API 契约（V1）

统一响应：

```json
{
  "code": 0,
  "msg": "ok",
  "timestamp": 1789520000000,
  "requestId": "...",
  "data": {}
}
```

### 6.1 `POST /v1/auth/platform-login`

请求：

```json
{
  "platform": "wechat",
  "code": "one-time-code",
  "anonymousCode": null,
  "clientVersion": "3.4.2"
}
```

响应保留前端当前所需字段，并新增 `serverTime`、`saveRevision`。过渡期可同时返回 `data`（JSON 字符串）和 `save`（对象），待前端切换完成后移除字符串字段。

### 6.2 `GET /v1/save`

- Header：`Authorization: Bearer <token>`。
- 返回 `exists/revision/serverSavedAt/save`。
- 无存档返回 `exists: false`，不把空对象伪装成有效存档。

### 6.3 `PUT /v1/save`

请求包含 `baseRevision`、`clientVersion`、`clientSavedAt`、`save`、`idempotencyKey`。

响应包含新 `revision/serverSavedAt/hash`。冲突返回 HTTP 409 + `SAVE_CONFLICT`，前端不得自动覆盖，应保留本地数据并在下次启动重新比较；V1 不做字段级自动合并。

### 6.4 `GET /v1/bootstrap-config`

返回维护状态、最低兼容版本、公告摘要和功能开关；可匿名访问，强缓存。

### 6.5 `POST /v1/redeem`（可选）

服务端校验并事务发奖；不能只返回奖励让客户端自行永久入账。

### 6.6 不提供的接口

- 不提供每分钟心跳。
- 不提供客户端任意读写数据库能力。
- 不提供按 `playerId` 查他人存档的接口。
- 不把平台 `openid/session_key/AppSecret` 返回给客户端。

## 7. 前端配套改造计划

1. 为 `PlatformRegistry` 明确安装微信/抖音类型；当前登录路径在抖音构建下还需确认 `EPlatform.ByteDance`，不能落入 Web。
2. 恢复 `zysdk/http.ts`，实现统一 HTTPS 请求、5 秒超时、Bearer token、错误码映射和 requestId。
3. 改造 `zysdk/user.ts`：平台登录、拉取存档、保存存档。
4. `LoadingActivity` 保持“登录失败按游客启动”，但成功后用 `playerId` 选择正确本地命名空间，再执行云/本地存档决策。
5. `ServerController`：
   - 去掉 1 分钟心跳；
   - 保存触发改为“关键节点 + 退后台 + 5～10 分钟脏数据合并”；
   - 同一时刻只允许一个上传，后续变更合并为下一轮；
   - 失败使用指数退避，不阻塞 UI；
   - 成功后再清 `serverDirty`，并保存最新 revision。
6. 将存档比较从纯客户端时间升级为 `revision + serverSavedAt`；保留旧 `time` 规则用于兼容迁移。
7. token 和 revision 使用独立本地键保存，不混进可上传的 `modules`。
8. 增加云状态诊断：游客/已登录、最近成功同步时间、待同步、冲突、离线。

## 8. 安全与一致性基线

- 全链路 HTTPS；平台请求域名加入小游戏合法域名白名单。
- AppSecret、数据库凭据仅放云端 Secret/环境变量；仓库只提交 `.env.example`。
- token 不写明文数据库、不写日志；日志脱敏 code、openid、session key、存档正文。
- 所有写接口做鉴权、schema 校验、大小限制、单玩家限流和幂等控制。
- 玩家只能访问由 token 解析出的自己数据，拒绝请求体中的任意 `playerId`。
- 登录接口按 IP + 设备摘要 + platform code 限流；存档接口按 player 限流。
- 服务端使用 UTC 毫秒时间；展示时再由前端转换时区。
- 每日奖励/签到在 V1 仍由客户端状态驱动，属于可作弊范围；上线前需明确接受该风险。
- 数据库至少每日自动备份；每月做一次真实恢复演练。

## 9. 免费资源控制策略

1. 函数/服务允许缩容到零，不开预置并发。
2. 登录时 1 次读玩家 + 1 次读存档；没有脏数据不上传。
3. 取消固定心跳；服务端时间随任一正常响应返回。
4. 存档聚合为单文档/单行，使用条件写，不拆成大量模块读写。
5. 配置接口强缓存；静态公告尽量放 CDN/静态托管。
6. 日志默认只保留结构化摘要，不记录存档正文；设置较短保留期。
7. 历史存档每人只保留 1 份，定期清理过期 session 和幂等键。
8. 控制台设置 50%、80%、95% 用量告警；生产默认不开无限按量付费。
9. 建立降级开关：云存档不可用时客户端继续本地运行并显示“待同步”。

粗略请求模型（用于压测，不代表平台实际额度）：

```text
单个日活玩家：1 次登录 + 1 次拉档 + 0～3 次合并保存
10,000 DAU：约 20,000 次读 + 最多 30,000 次写/日
```

上线前把实测 DAU、平均存档字节数、每日保存次数代入两个平台控制台的当期计费规则，不在代码里假设“永远免费”。

## 10. 实施阶段与验收

### 10.0 每个阶段强制执行的交付闭环

从 P0 开始，每一个阶段、每一个可独立交付的接口都必须依次经过以下四个步骤。任何一步未完成，该阶段不得标记为“完成”，也不得直接进入下一阶段。

#### A. 审计

- 阅读本阶段涉及的前端调用链、后端既有代码、数据表、环境配置和上一阶段验收记录。
- 执行 `git status`、检查当前分支和远端，识别并保护用户已有未提交改动。
- 列出本阶段会修改的文件、接口、数据结构、前端页面/服务和可能受影响的旧功能。
- 核对微信、抖音当期官方接口与云产品限制；对于可能变化的平台规则，不依赖旧记忆。
- 给出风险清单、兼容策略、回滚方式和验收用例，审计结论写入 `docs/audits/`。
- 如果需求、接口或数据模型发生变化，先同步修订：后端计划、OpenAPI、错误码、数据库文档和管理后台开发计划，再开始编码。

#### B. 执行

- 先补契约测试或失败用例，再实现后端功能。
- 后端接口实现完成后，在同一阶段内改造前端对应调用位置，不把前端接入推迟到项目最后。
- 同步更新 OpenAPI、JSON Schema、示例请求、环境变量说明、数据库迁移和运维说明。
- 只修改本阶段范围内文件，不夹带无关格式化、资源或历史改动。
- 涉及数据库结构时必须提供可重复执行的迁移与回滚方案，禁止直接手工改生产库后不留记录。

#### C. 验收

- 后端：lint、typecheck、单元测试、集成测试、数据库迁移测试和 `git diff --check` 全部通过。
- 接口：启动真实服务并发送真实 HTTP 请求，保存状态码、响应体摘要、requestId 和数据库结果；不能只以 mock 或编译通过作为证明。
- 前端：对应 Cocos 调用链完成静态检查、自动化测试、开发者工具联调；平台能力必须在微信/抖音真机补验。
- 回归：验证离线降级、超时、重复请求、鉴权失败、存档冲突、旧版本兼容和进程被杀等异常路径。
- 数据：确认没有密钥、token、openid、session key、完整存档或用户隐私进入日志和 Git。
- 输出本阶段验收报告到 `docs/acceptance/`，明确“已验证”“仅静态验证”“待真机验证”，禁止模糊写成全部完成。

#### D. 提交与推送

- 验收通过后才允许提交；提交前再次检查 `git status` 和 staged diff，只暂存本阶段文件。
- Git 提交说明使用中文，且要能说明业务结果，建议格式：

```text
阶段P2：完成微信与抖音平台登录及会话签发

- 接入两平台 code2Session 并统一错误映射
- 新增玩家和会话持久化及安全校验
- 完成前端登录接口接入与游客降级验证
- 补充接口契约、自动化测试和验收记录
```

- 每个阶段原则上一个可回滚提交；如果后端与前端处于两个 Git 仓库，则分别提交，但提交说明必须带相同阶段号和关联说明。
- 提交成功后推送到当前远端分支，并记录 commit hash、远端分支、推送结果和是否存在未提交文件。
- 推送失败时阶段状态为“本地已提交、未交付”，不得写成完成。
- 未经用户明确要求，不合并主分支、不打生产标签、不发布生产环境。

### 10.0.1 后端接口与前端接入的完成定义

每个业务接口采用纵向交付，不允许只完成后端：

```text
后端契约审计
  -> 后端接口与数据库实现
  -> 真实请求验证
  -> 前端 SDK/Service 接入
  -> 前端页面或启动链联调
  -> 微信/抖音对应环境验收
  -> 两个仓库分别中文提交并推送
```

接口交付清单至少包含：

| 后端能力 | 前端必须同步完成 | 联合验收 |
|---|---|---|
| 平台登录 | `PlatformRegistry`、`LoadingActivity`、`zysdk/user.ts` | 真 code 登录、游客降级、token 持久化 |
| 获取云存档 | 启动阶段命名空间切换及 `checkServerData` | 本地/云端新旧档选择、版本迁移失败回退 |
| 保存云存档 | `ServerController` 合并上传、revision 保存 | 退后台、关键节点、离线重试、多设备 409 |
| 远程配置 | `ChannelConfigLoader` 或独立 ConfigService | 缓存、超时、本地默认值、维护开关 |
| 兑换码（可选） | `RedeemWindow` 的真实 redeem 回调 | 幂等、过期、重复领取、事务发奖 |
| 封禁状态 | 启动提示及重试/退出行为 | 临时封禁、永久封禁、到期恢复 |

管理后台不与这些接口同步开工，但后端开发阶段必须同步维护后台计划中的字段映射、权限需求和待开发页面清单，确保后端完成后可以直接进入后台实施。

### P0：云产品与契约确认（1～2 天）

- 在微信、抖音控制台确认认证方式、运行时、数据库、免费/试用额度、到期和备份能力。
- 确定两个环境域名、AppId、Secret 管理和合法域名。
- 固化 OpenAPI、错误码和 JSON Schema。

验收：形成配置矩阵；任何密钥均未进入仓库；明确半年试用期内的配额、到期日、告警和试用结束后的费用/停服方案；完成审计报告、中文提交并推送。

### P1：工程骨架（1～2 天）

- 初始化 Node.js + TypeScript 单体工程。
- 建立 `app/domain/platform/repository/routes` 分层。
- 加入 lint、typecheck、unit test、环境配置校验、健康检查。
- 提供内存 repository 便于本地测试。

验收：本地可启动，`/health` 可用，缺少必要环境变量时快速失败；真实请求验证通过；完成验收报告、中文提交并推送。

### P2：双平台登录（2～4 天）

当前状态（2026-09-16）：P2A 本地实现与前端联合测试通过；P2B1 的 MongoDB 外连方案经真实产品核对后判定不适用。P2B2 已改用 CloudBase 官方 JS SDK 的 HTTP API，并完成微信 CloudBase 内置文档数据库的真实写入、读取和清理验收。P2 仍需有效微信 code、开发者工具和真机验收。

部署进度：P2C1 已完成 CloudBase Run 容器部署包与本地生产镜像验收；P2C2 等待 CloudBase CLI/控制台账号授权后创建 `miao-travel-wechat` 服务、配置云端 Secret 和默认 HTTPS 域名。

- 实现微信、抖音 code2Session adapter。
- 玩家 upsert、session 签发/刷新/撤销、封禁检查。
- 增加平台 API 超时、重试边界和错误映射。
- P2B 使用 `PERSISTENCE_DRIVER=cloudbase-http` 选择云仓储，本地测试仍使用 `memory`；服务端 API Key 只放本地或云端 Secret。
- 使用通过依赖审计的 `@cloudbase/js-sdk@3.9.4` 调用 CloudBase HTTP API；有高危传递依赖的旧 `@cloudbase/node-sdk` 和不适用的 MongoDB 外连实现均不进入交付版本。
- `players`、`sessions` 集合已由验收脚本确保存在；会话索引和物理过期清理在 P3 随鉴权与云存档模型一起配置和验收。

验收：code 不可复用；相同平台账号返回同一 playerId；两个平台数据隔离；Secret/session key 不出现在响应和日志中；前端登录和游客降级已接入并完成开发者工具/真机验证；后端和前端分别中文提交并推送。

### P3：云存档 MVP（3～5 天）

- 实现 get/put、revision 条件更新、幂等、大小和结构校验、上一版本备份。
- 前端恢复 HTTP/User/ServerController 链路。
- 保持登录/云服务失败时本地启动。

验收：新用户、仅本地档、仅云档、本地较新、云端较新、同 revision、坏档、版本迁移失败、多设备冲突全部有自动测试；真实服务和数据库请求通过；前端读取、保存、离线重试和冲突处理完成；两个仓库分别中文提交并推送。

### P4：平台联调与故障演练（2～4 天）

- 微信开发者工具、抖音开发者工具和真机分别联调。
- 模拟超时、500、配额耗尽、离线、重复请求、退后台、进程被杀。
- 验证域名白名单、HTTPS、日志脱敏和用量告警。

验收：任何后端故障都不导致本地档丢失；多次重试不重复写/发奖；换设备可恢复；微信和抖音验收证据归档；相关提交全部推送。

### P5：可选运营能力（2～3 天）

- 远程公告/开关。
- 若首发需要，再做兑换码事务发奖和 CLI 管理工具。

验收：后端接口、前端入口、异常提示、幂等和权限验证闭环；同步管理后台计划；中文提交并推送。

### P6：灰度上线（至少 3 天观察）

- 先白名单账号，再 5%/20%/100% 灰度。
- 观察登录成功率、P95 延迟、存档失败率、冲突率、平均存档大小和资源消耗。
- 达到回滚阈值立即关闭云同步开关，客户端继续本地档。

验收：灰度指标和回滚演练达标；发布记录、监控截图/数据摘要和最终验收报告归档；版本提交和标签策略经用户确认后执行。

## 11. 测试清单

### 自动化测试

- 平台 adapter：成功、无效 code、超时、平台限流、异常响应。
- 鉴权：缺 token、过期、撤销、伪造、封禁。
- 存档：schema、大小、hash、revision 冲突、幂等、并发条件写。
- 迁移：`3.4.2` 及后续版本存档；未知模块忽略但不破坏已知模块。
- 安全：越权读写、原型污染键、超深 JSON、超大数组、日志脱敏。
- 降级：数据库失败、平台 API 失败、网络断开时前端仍可进入游戏。

### 真机验收

1. 微信、抖音各自首次进入并产生不同 playerId。
2. 完成关卡、改变金币/体力/星星、任务、签到后退后台，云端 revision 增加一次。
3. 同账号第二台设备启动，恢复等级、资源、任务、活动和续玩记录。
4. 两台设备制造冲突，旧 revision 保存收到 409，云端新档不被覆盖。
5. 关闭网络完成一局，确认本地落盘；恢复网络后合并上传。
6. 后端停服或额度耗尽时仍能进游戏，且明确显示待同步。
7. 重复点击、重复回调、请求超时重试均不重复发奖。

## 12. 监控指标与上线门槛

核心指标：

- 登录成功率、平台 code2Session 失败率、P50/P95/P99 延迟。
- 云存档读取/写入成功率、409 冲突率、平均与 P95 存档大小。
- 数据库读写次数、函数执行次数/时长、公网流量和每日成本。
- 5xx、限流、配额不足、备份失败和异常增长告警。

建议上线门槛：

- 非平台外部故障下登录成功率 >= 99.5%。
- 存档 API 成功率 >= 99.9%，无静默覆盖案例。
- P95 登录 < 1.5 秒，P95 存档读写 < 800 毫秒（同地域目标）。
- 所有关键故障演练通过，且可以用上一版本存档完成恢复。

## 13. 建议工程目录

```text
miao-travel-server/
├── src/
│   ├── app.ts
│   ├── config/
│   ├── domain/
│   │   ├── auth/
│   │   ├── player/
│   │   ├── save/
│   │   └── redeem/          # 可选
│   ├── platform/
│   │   ├── wechat/
│   │   └── bytedance/
│   ├── repositories/
│   ├── routes/v1/
│   ├── security/
│   └── observability/
├── test/
├── deploy/
│   ├── wechat/
│   └── bytedance/
├── scripts/
├── docs/
│   ├── api.md
│   ├── data-model.md
│   └── runbook.md
├── .env.example
├── package.json
└── tsconfig.json
```

## 14. 首轮开发任务顺序

按以下顺序执行，避免先做无用基础设施：

1. 控制台核实两个平台云资源和计费，完成 P0 配置矩阵。
2. 冻结登录、云存档 API/错误码/JSON Schema。
3. 搭建单体 TypeScript 工程与内存 repository。
4. 先打通微信登录，再复用契约接抖音 adapter。
5. 完成 revision 云存档和并发测试。
6. 改造前端 stub，完成本地优先和离线降级。
7. 双平台真机测试、故障演练、用量压测。
8. 白名单灰度上线。

V1 完成标准不是“接口能调用”，而是：在任意网络失败、平台登录失败、多设备竞争和旧版本存档场景下，都不会静默丢档，并且免费资源消耗可被观测、限制和随时降级。

## 15. 官方资料核对入口

- 抖音小游戏 `tt.login`：临时 code 有效期短，服务端用它换取 openid/session key；冷启动应避免重复调用。
- 抖音小游戏 `code2Session`：AppSecret 和 session key 仅能留在服务端。
- 抖音小游戏网络请求：生产请求需 HTTPS，并配置合法域名白名单。
- 抖音云托管：支持小游戏后端托管和免登录能力，但部署前仍需以当前控制台实际开放能力为准。
- 腾讯 CloudBase：支持云函数、数据库、存储和小游戏场景；当前定价为套餐/资源点/按量体系，免费体验存在期限与配额，不应视作永久免费的生产承诺。
