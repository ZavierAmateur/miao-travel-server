# 喵的旅行日记管理后台开发计划（基于后端 V1）

> 文档日期：2026-09-16
>
> 前置项目：`/Volumes/work/Projects/Server/miao-travel-server`
>
> 前端项目：`/Volumes/work/Projects/cocos/喵的旅行日记`
>
> 启动时机：后端 V1 接口、数据模型和部署形态稳定并完成验收后
>
> 计划属性：活文档。后端开发期间若接口、字段、错误码、权限或存储策略变化，本计划必须在同一阶段同步修订。

## 1. 建设目标

管理后台用于安全、低成本地完成玩家数据查询、问题定位、错误日志查看和必要的运营操作。它不是玩家侧功能，也不是后端数据库的通用编辑器。

V1 管理后台重点解决：

1. 按 playerId、平台和受控的账号标识查找玩家。
2. 查看玩家基础状态、云存档摘要、同步版本和最近活动。
3. 查看后端错误日志、requestId 链路和接口健康情况。
4. 执行封禁/解封、存档历史查看与受控回滚。
5. 管理远程公告和功能开关；若后端启用兑换码，再增加兑换码管理。
6. 所有敏感读取和修改都经过权限校验、二次确认和审计记录。

## 2. 与后端计划的联动规则

### 2.1 单一事实来源

- 后端 OpenAPI、数据库迁移、错误码文档和权限定义是后台实现的唯一契约来源。
- 本文只规划后台页面、交互和运维流程，不复制一份容易过期的完整后端结构。
- 后台生成 API Client；禁止页面手写与后端不一致的请求/响应类型。

### 2.2 后端变更同步门禁

后端每个阶段审计时必须填写“后台影响检查”：

| 后端变更 | 必须同步到后台计划 |
|---|---|
| 新增/删除/重命名字段 | 列表列、详情字段、筛选项、脱敏和导出规则 |
| API 路径或响应变化 | 页面调用、生成 Client、错误提示和兼容版本 |
| 新错误码 | 日志筛选、中文解释和处理建议 |
| 数据库迁移 | 查询能力、历史数据兼容和回滚展示 |
| 权限变化 | RBAC 权限点、按钮可见性和后端强校验 |
| 新增敏感数据 | 脱敏、访问审批、日志禁记规则 |
| 新运营能力 | 新页面、表单校验、预览和发布/回滚流程 |

同步要求：

- 后端 PR/提交包含 `后台影响：无/有`；为“有”时必须同时修改本文或 `docs/admin-contract.md`。
- 变更后更新“页面—接口—权限矩阵”和后台待办，不能等后台正式开发时再补。
- 后端破坏性变更必须提供兼容窗口；管理后台未升级前，旧接口不能直接删除。
- 后台正式开发前执行一次契约冻结审计，清理所有待确认项。

## 3. 范围与非范围

### 3.1 V1 范围

- 管理员登录和 RBAC。
- 仪表盘与服务健康摘要。
- 玩家检索与详情。
- 云存档查看、版本比较和回滚。
- 封禁与解封。
- 错误日志与 requestId 查询。
- 远程配置/公告管理。
- 管理操作审计日志。
- 兑换码管理（仅后端启用时）。

### 3.2 V1 不做

- 不提供任意 SQL/数据库控制台。
- 不允许直接修改任意 JSON 存档字段。
- 不做复杂 BI、数据仓库、用户画像或实时大屏。
- 不在后台展示平台 AppSecret、session key、明文 token。
- 不把完整玩家存档写入浏览器日志、埋点或错误上报。
- 不做支付、邮件、排行榜页面，除非后端范围正式增加。

## 4. 推荐技术架构

为了降低维护成本，建议管理后台作为同一后端仓库中的独立 Web 应用，复用类型、权限和部署流程：

```text
miao-travel-server/
├── apps/
│   ├── api/                 # 玩家 API + 管理 API
│   └── admin-web/           # 管理后台前端
├── packages/
│   ├── contracts/           # OpenAPI 生成类型/公共 schema
│   └── admin-permissions/   # 权限常量
└── docs/
```

建议：React + TypeScript + Vite + 成熟组件库。后台访问量低，构建后放静态托管；所有数据必须通过 `/admin/v1/*` 服务端接口访问，浏览器不能直连数据库。

如果后端最终不是 monorepo，则后台可独立仓库，但仍通过固定版本的 OpenAPI 生成 Client，并在两个仓库记录关联 commit。

## 5. 管理员认证与权限

### 5.1 认证

- 优先使用平台云的 IAM/企业身份；不可用时才自建管理员账号。
- 管理入口不开放注册；首个管理员通过部署环境安全初始化。
- 强制 MFA、短会话、空闲超时和重新认证。
- 管理 token 使用 HttpOnly + Secure + SameSite Cookie；不放 localStorage。
- 登录失败限流，记录 IP、设备摘要和 requestId，但不记录密码。

### 5.2 RBAC 角色

| 角色 | 权限 |
|---|---|
| `viewer` | 查看脱敏玩家摘要、指标和错误日志 |
| `support` | 查看玩家详情、同步状态，执行有限诊断 |
| `operator` | 管理公告/开关、兑换码、临时封禁 |
| `admin` | 用户权限、永久封禁、存档回滚等高风险操作 |

权限由后端强制校验。前端隐藏按钮只改善体验，不能作为安全边界。

## 6. 页面与功能计划

### 6.1 登录页

- 管理员登录、MFA、登录失败提示。
- 不暴露“账号是否存在”等可枚举信息。

### 6.2 仪表盘

- 今日/近 7 日登录用户数。
- 登录成功率、存档读写成功率、409 冲突率。
- P95 延迟、5xx、平台 code2Session 失败。
- 数据库读写、函数调用、流量和试用配额使用率。
- 最近告警和当前维护/降级开关。

V1 只展示聚合指标，不扫描全量玩家表临时计算；指标由后端定时聚合或云监控提供。

### 6.3 玩家列表

- 筛选：playerId、平台、状态、注册/最近登录时间、客户端版本。
- openid 只允许精确查询且默认脱敏；禁止模糊遍历。
- 列表展示：playerId、平台、状态、等级、最近登录、云存档 revision、最近同步时间。
- 游标分页，禁止大 offset 和全量导出。

### 6.4 玩家详情

分区展示：

- 基础信息：平台、版本、创建/登录时间、封禁状态。
- 资源摘要：等级、体力、金币、星星、道具计数。
- 旅程和收集：进度、动物、任务、签到和活动摘要。
- 云同步：revision、保存时间、大小、hash、客户端版本。
- 最近错误和管理操作。

默认只读。V1 不提供直接编辑金币、体力等任意字段的通用表单。

### 6.5 云存档诊断与回滚

- 展示当前版本和上一历史版本的结构化摘要。
- 对比模块、revision、时间、大小和 hash；敏感正文按权限按需加载。
- 回滚前二次确认并填写原因。
- 回滚不是简单覆盖：服务端产生新的 revision，记录来源 revision、操作者和理由。
- 回滚后提示玩家下次启动的同步影响。
- V1 当前版和上一版位于同一 `cloud_saves` 文档，上一版字段为 `previous`；后台不得依赖单独的 `cloud_save_history` 集合。

### 6.6 封禁管理

- 临时/永久封禁、原因、到期时间、内部备注。
- 解封同样必须记录原因。
- 高风险永久封禁仅 `admin` 可执行，并要求二次确认。
- 玩家下一次鉴权/刷新时生效，后台显示传播状态。

### 6.7 错误日志中心

查询维度：

- 时间范围、环境、平台、接口、HTTP 状态、业务错误码。
- requestId、playerId（精确）、客户端版本、服务版本。
- 错误级别、异常类型和处理状态。

详情展示：时间线、脱敏请求摘要、响应错误码、堆栈、关联部署版本。禁止展示 token、code、openid、session key 和完整存档正文。

支持给错误添加状态：`new/investigating/resolved/ignored`、负责人和内部备注。V1 不自建海量日志数据库；优先查询云日志，后台只保存处置状态和索引引用。

### 6.8 远程配置与公告

- 草稿、预览、发布、回滚。
- 指定平台、环境、客户端版本范围和生效时间。
- JSON Schema 校验，保存前展示差异。
- 维护开关、云存档降级开关属于高风险项，必须二次确认并写审计日志。

### 6.9 兑换码（条件启用）

- 创建批次、奖励模板、有效期、总次数和单玩家限制。
- 查看使用数量和失败原因分布。
- 发布后不允许无审计地修改奖励；需停用旧批次并创建新批次。
- 不展示或导出尚未发放的完整通用码列表给低权限角色。

### 6.10 审计日志

- 记录管理员登录、查看敏感信息、封禁、解封、回滚、配置发布和权限变更。
- 字段包括操作者、动作、对象、前后摘要、原因、时间、IP、requestId、结果。
- 审计记录追加写，普通管理员不可修改或删除。

## 7. 管理 API 初步清单

| 页面 | 管理 API | 权限 |
|---|---|---|
| 登录 | `POST /admin/v1/auth/login`、`logout`、`me` | public/admin-self |
| 仪表盘 | `GET /admin/v1/dashboard` | `metrics:read` |
| 玩家列表 | `GET /admin/v1/players` | `player:read` |
| 玩家详情 | `GET /admin/v1/players/:id` | `player:read` |
| 存档版本 | `GET /admin/v1/players/:id/saves` | `save:read` |
| 存档回滚 | `POST /admin/v1/players/:id/save-rollback` | `save:rollback` |
| 封禁/解封 | `POST /admin/v1/players/:id/ban`、`unban` | `player:ban` |
| 错误日志 | `GET /admin/v1/errors`、`/:id` | `error:read` |
| 错误处置 | `PATCH /admin/v1/errors/:id/status` | `error:manage` |
| 远程配置 | `GET/POST/PATCH /admin/v1/configs` | `config:read/write` |
| 配置发布 | `POST /admin/v1/configs/:id/publish` | `config:publish` |
| 兑换码 | `/admin/v1/redeem-batches` | `redeem:*` |
| 审计日志 | `GET /admin/v1/audit-logs` | `audit:read` |

最终路径以后端冻结的 OpenAPI 为准。

## 8. 后端在 V1 阶段必须提前预留的能力

即使后台稍后开发，后端也必须在开发过程中准备：

1. 稳定的内部 playerId 和按平台唯一索引。
2. 结构化错误码、requestId 和服务版本。
3. 脱敏结构化日志，支持按 requestId/playerId 精确关联。
4. 云存档 revision、历史版本和回滚所需元数据。
5. 玩家状态/封禁字段及鉴权时检查。
6. 管理操作审计表或可靠审计日志接口。
7. 管理 API 与玩家 API 分离的路由、鉴权和限流策略。
8. 指标聚合接口，避免后台直接扫描业务数据库。
9. OpenAPI 及权限点定义，供后台生成类型。

## 9. 开发阶段

每个后台阶段同样执行“审计 → 执行 → 验收 → 中文提交并推送”。未推送不得标记完成。

### A0：后端完成后的契约冻结审计（1～2 天）

审计：核对实际 OpenAPI、迁移、错误码、日志字段、RBAC 和部署方式，逐项修正本文。

执行：生成页面—接口—字段—权限矩阵，冻结 V1 范围和原型。

验收：所有页面都有真实接口来源；不存在依赖前端猜测的字段；中文提交并推送。

### A1：后台工程与管理员安全（2～4 天）

执行：搭建 Web 工程、生成 API Client、登录/MFA、路由权限、基础布局、错误边界。

验收：未登录、过期、越权、CSRF、暴力登录和退出场景通过；安全头与 Cookie 设置正确；中文提交并推送。

### A2：玩家查询与详情（3～5 天）

执行：玩家列表、精确搜索、游标分页、详情和脱敏展示。

验收：微信/抖音数据隔离正确；低权限看不到敏感字段；大数据量查询有索引且无全表扫描；中文提交并推送。

### A3：云存档诊断、回滚与封禁（3～5 天）

执行：版本摘要、diff、回滚、封禁/解封、二次确认和理由收集。

验收：并发回滚有 revision 保护；所有写操作有审计；前端游戏能正确识别回滚后新 revision 和封禁状态；中文提交并推送。

### A4：错误日志与监控（3～5 天）

执行：日志搜索、详情、requestId 链路、处置状态和仪表盘指标。

验收：使用真实故障请求验证；敏感数据不泄露；查询时间范围和数量受限；中文提交并推送。

### A5：运营配置与可选兑换码（3～5 天）

执行：草稿/预览/发布/回滚、差异展示；按后端实际范围接兑换码。

验收：错误配置无法发布；发布与回滚都有审计；客户端真实拉取并按平台/版本生效；中文提交并推送。

### A6：整体安全与生产验收（2～4 天）

执行：权限矩阵回归、依赖扫描、性能测试、备份恢复、部署和回滚演练。

验收：所有高风险操作有强制二次确认与审计；生产环境无默认账号；用量告警开启；最终中文提交并推送。

## 10. 验收标准

- 类型检查、lint、单元/组件/E2E 测试通过，`git diff --check` 无错误。
- 所有管理 API 使用真实服务验证，不能只 mock。
- 权限同时验证“页面不可见”和“直接请求被后端拒绝”。
- 所有敏感信息经过脱敏，浏览器控制台和错误上报不含隐私。
- 关键写操作具备幂等、二次确认、原因记录、审计和回滚方案。
- 页面在常见桌面分辨率可用；后台以桌面端为主，不为移动端牺牲表格与诊断效率。
- 每阶段产生验收报告、中文 commit hash 和推送结果。

## 11. 后端开发期间的维护清单

后端每完成一个阶段，即使后台尚未开工，也必须在本文附属清单中确认：

- [ ] OpenAPI 是否变化。
- [ ] 数据库字段/索引是否变化。
- [ ] 错误码和日志字段是否变化。
- [ ] 新功能是否需要后台查看或操作。
- [ ] 是否新增敏感字段及脱敏规则。
- [ ] 是否新增 RBAC 权限点。
- [ ] 页面—接口—权限矩阵是否需要更新。
- [ ] 本计划是否已同步提交并推送。

后台真正开工时，应以最后一个后端 V1 commit 为基线重新审计；本文描述与实际代码冲突时，以已验收的后端契约为准，并先修订计划再执行。

## 12. 后端同步记录

### 2026-09-16 P2A 双平台登录

- 新增稳定 `playerId`，后续作为后台玩家查询主键。
- 玩家身份按 `(platform, appId, openId)` 隔离；openid 不通过玩家登录 API 返回。
- 新增登录错误码：`INVALID_REQUEST`、`PLATFORM_CODE_INVALID`、`PLATFORM_AUTH_REJECTED`、`PLATFORM_RESPONSE_INVALID`、`PLATFORM_AUTH_UNAVAILABLE`。
- 登录响应提供 `requestId`、`isNew`、封禁摘要和 `serverTime`；后台错误日志需支持这些错误码和 requestId。
- 当前玩家与会话仓储仍为内存实现，管理后台不得开工依赖；待 P2B/P3 固化云数据库模型后再次同步。

### 2026-09-16 P2B1 云持久化

- 玩家与会话已增加 MongoDB 持久化实现，目标为腾讯 CloudBase 文档型数据库；正式云环境尚未验收，后台仍不开始开发。
- `players._id` 为平台、AppId、openid 组合的 SHA-256，不保存明文 openid；内部 `players.id` 保持唯一，继续作为后台主查询键。
- 管理后台若按 openid 精确查询，必须把输入提交给管理 API，由后端执行相同哈希后精确匹配；浏览器不得直接得到哈希算法上下文、数据库连接或全量哈希值。
- `unionId` 只保存哈希；自有会话以 token 哈希作为 `_id`，并通过 TTL 索引自动清理过期会话。
- 后台玩家列表可使用 `(platform, status, lastLoginAt)` 索引；禁止按 openid 模糊搜索。
- P2B2 云端验收后需补记 CloudBase 实际索引、备份/回档能力、日志入口和环境隔离方式。

> P2B1 的 MongoDB 外连实现已在 P2B2 被替换，以下 P2B2 记录为当前有效契约。

### 2026-09-16 P2B2 CloudBase HTTP 持久化

- 后端已改用 `cloudbase-http` 驱动，通过官方 JS SDK 访问 CloudBase 内置文档数据库；后台不得依赖 Mongo 连接串、Mongo 管理接口或浏览器直连数据库。
- 微信环境 `players`、`sessions` 集合已完成真实写入、读取和探测数据清理验收；抖音环境尚未验收。
- `players._id` 仍为平台身份组合哈希；内部 playerId 改为由该哈希单向派生的稳定 UUID 形态，可继续作为后台精确查询主键。
- `sessions` 已保存 `expiresAt`，但 TTL/查询索引尚未在真实环境验收；后台不得假设过期文档已被物理删除，应以服务端鉴权结果为准。
- 管理后台的错误日志需能关联 CloudBase requestId，但不得显示服务端 API Key、环境 Secret、openid、unionid 或 token。
- 后台仍不开始开发；待 P3 云存档结构、索引和管理 API 契约稳定后再次同步。

### 2026-09-16 P2C1 CloudBase Run 部署包

- 玩家 API 将以独立容器服务 `miao-travel-wechat` 部署，首版最小副本 0、最大副本 1；管理后台不能直接复用该服务的玩家鉴权作为管理员鉴权。
- 本阶段没有 API 或数据库字段变化；后台计划无需调整页面契约。
- 后续后台 Web 和管理 API 应独立评估访问策略、管理员 Secret、MFA 和公网暴露范围，不把玩家 API 的 `PUBLIC` 策略直接复制过去。

### 2026-09-16 P2C2 云端基础部署

- 微信玩家 API 已部署为 `miao-travel-wechat`，当前使用最低容器规格、0～1 副本和公网 HTTPS 域名。
- CLI 服务详情可能返回环境变量原文；未来后台运维功能和自动化不得记录、展示或转发 `EnvParams`。
- 本阶段没有玩家 API 字段变化；后台仍等待云存档与管理 API 契约稳定后开工。
- 轮换凭据后的 `002` 版本已承载 100% 流量，CloudBase 真实读写探测通过；本次没有 API、数据库字段、错误码、RBAC 或页面需求变化。
- 微信游戏端改用 `wx.cloud.callContainer`，但管理后台不是微信运行环境，未来仍需使用独立管理 API 公网域名、自定义域名或网关；不能复用小游戏的免域名调用方式。

### 2026-09-16 P2C3 微信首次登录故障修复

- CloudBase 按主键读取不存在文档会抛出 `DOCUMENT_NOT_FOUND`；玩家和会话仓储现将该错误映射为“未找到”，未知数据库错误仍上抛。
- 本次不改变玩家 API、数据库字段、公开错误码、RBAC 或页面需求，后台页面—接口—权限矩阵无需调整。
- 后台日志页未来应区分正常的“记录不存在”与真正数据库故障；正常首次登录查无玩家不应产生 5xx 告警。
- 修复版本云端验收完成：首次登录创建玩家、同一账号二次登录复用 playerId 均通过；本次仍没有管理 API 或后台页面契约变化。
- 调试截图可能泄露玩家会话 token；后台日志和诊断页面必须默认脱敏，任何复制/导出能力也不得输出完整 token。

### 2026-09-16 P3A 云存档契约冻结

- `cloud_saves._id` 使用内部 `playerId`，当前版本字段为 `revision/save/hash/sizeBytes/clientVersion/clientSavedAt/serverSavedAt`。
- 最近一次幂等键和请求哈希仅用于玩家写接口去重，后台页面默认不展示完整幂等键。
- 每个文档仅内嵌一个 `previous` 上一版本快照；后台“当前/上一版对比”和回滚页面据此读取，不建设 `cloud_save_history` 集合。
- 玩家端仅允许 `settings/tutorial/user/task/activitys` 五个模块，存档上限 512 KiB；后台详情需展示白名单校验结果和大小，不允许绕过校验直接编辑正文。
- 新增玩家错误码：`AUTH_REQUIRED`、`SESSION_INVALID`、`SESSION_EXPIRED`、`INVALID_SAVE`、`SAVE_TOO_LARGE`、`SAVE_CONFLICT`。错误日志中心需要支持按这些业务码过滤。
- `SAVE_CONFLICT` 只返回云端 `revision/serverSavedAt/hash` 摘要，不返回完整存档；后台跨版本诊断仍必须通过独立管理员 API 和权限点访问正文。

### 2026-09-16 P3A 云端部署进度

- 微信 CloudBase Run 已部署云存档版本，健康检查和未鉴权拒绝均通过；不新增管理 API、RBAC 或后台页面契约。
- 真实玩家已完成 revision 2 云存档端到端读写验收，P3A 契约与数据结构正式稳定。
- 管理后台仍不开始实施；待 P4 故障演练完成后，再根据最终日志字段与运维入口复审 A1～A3 页面。
